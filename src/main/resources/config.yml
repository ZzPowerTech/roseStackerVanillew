# Worlds where RoseStacker mob/entity stacking will be blocked.
blocked-worlds:
  - vanillew

# Optional debug logs for each blocked mob stack event.
debug:
  blocked-stack: false

# Hard cap for physical living entities per chunk in blocked worlds.
limits:
  chunk-living-entities:
    enabled: true
    max: 24

